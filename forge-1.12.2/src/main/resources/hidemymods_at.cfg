# Access transformer file required by the FMLAT manifest attribute.
# Empty for now: the ASM transformer rewrites a method body but does
# not need any access widening on the target. The file must exist
# because FML resolves the FMLAT attribute eagerly at coremod load
# and a missing resource aborts startup.
